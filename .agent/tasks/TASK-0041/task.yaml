protocol_version: 3

task_id: TASK-0041
task_revision: 2
state: APPROVED

origin:
  type: correction
  task_id: TASK-0041
  gap_ids: [POST_SAMPLED_PROGRESSION_HANDOFF, GENERATED_CANONICAL_ARTIFACT_DRIFT]

architect_binding:
  target_repository: phatnguyen03022001/ilets

target:
  repository: phatnguyen03022001/ilets
  branch:
    name: dev
    role: primary-development

execution_base:
  mode: handoff_snapshot
  capture: after_final_planning_commit
  require_exact_match: true

skill_library:
  repository: phatnguyen03022001/agent-skills
  revision: "76f2d3877d1698f91eb5a6117e2301ed49480379"

execution_skills:
  required: [executor, simplicity, verification]
  recommended: []

inherited_evidence:
  revision_1_semantic_candidate: "84139d4433bf62635aa501c57c8bcb19dc8e0b3b"
  revision_1_failed_boundary: "b321b4aefab00a3b6247f16ed78bb7fbdb508eed"
  revision_1_actions_run: 33590220228
  failure_class: GENERATED_CANONICAL_ARTIFACT_DRIFT
  exact_failure: "canonical materialization failed: generated artifact drift: tools/canonical/generated/reading-training-registry.json"
  binding_status: >-
    Exact boundary binding, detached candidate checkout, and exact HEAD assertion passed before ./verify failed.
    Revision 2 must preserve the revision-1 semantic bytes and correct only the deterministic generated artifact
    required by canonical materialization.

authority_sources:
  - source: "phatnguyen03022001/ilets@84139d4433bf62635aa501c57c8bcb19dc8e0b3b"
    role: "Immutable revision-1 semantic implementation candidate to preserve exactly."
    precedence: 1
  - source: "GitHub Actions run 33590220228 attempt 1"
    role: "Authoritative proof that revision 1 failed only at generated canonical artifact drift after exact candidate binding."
    precedence: 2
  - source: "phatnguyen03022001/ilets@84139d4433bf62635aa501c57c8bcb19dc8e0b3b:docs/data/evidence-progression-content.md"
    role: "Canonical source whose blob/provenance changed and requires deterministic registry rematerialization."
    precedence: 3

objective: >-
  Complete TASK-0041 without changing its accepted semantic design by rematerializing the deterministic
  canonical generated registry required by the revision-1 canonical-doc mutation, then verify the resulting
  exact immutable candidate through one new sentinel boundary and publish the terminal report.

scope:
  required_changes:
    - "Refresh exact refs/topology/task and require dev equals the exact revision-2 handoff base with topology exactly {dev, main}."
    - "Treat revision-1 semantic candidate 84139d4433bf62635aa501c57c8bcb19dc8e0b3b as immutable semantic evidence. Do not amend, revert, replay, reformat, or reinterpret its seven semantic files."
    - "Generate repository-native deterministic canonical materialization for the current tree and update tools/canonical/generated/reading-training-registry.json to exactly the output required by the canonical materializer."
    - "The generated correction must reflect current canonical source revision/provenance, including the changed docs/data/evidence-progression-content.md source blob, without hand-editing semantic registry entries."
    - "If repository-native materialization deterministically changes any additional already-tracked file beyond tools/canonical/generated/reading-training-registry.json, stop AUTHORITY_REQUIRED and report the exact additional path instead of expanding scope silently."
    - "Create exactly one revision-2 implementation candidate from the exact handoff base. Its only non-task semantic/derived change must be tools/canonical/generated/reading-training-registry.json."
    - "Then create exactly one sentinel-only verification-boundary child changing .github/verification-boundary/candidate to the revision-2 candidate SHA."
    - "Require repository-native GitHub Actions verify to bind the exact new candidate, checkout it detached, assert exact HEAD, and complete ./verify PASS."
    - "Do not rerun or mutate the failed revision-1 run; revision 2 gets one new exact verification boundary/run."
    - "Only after authoritative PASS publish exactly one report-only child at .agent/tasks/TASK-0041/report.yaml and stop at NEEDS_REVIEW."

  forbidden_changes:
    - "Do not modify any of the seven revision-1 semantic files: docs/data/evidence-progression-content.md, docs/behavior/planner-and-lifecycle.md, services/core-api/internal/progression/sampled_reading.go, services/core-api/internal/progression/sampled_reading_test.go, services/core-api/internal/planner/kernel_test.go, services/core-api/internal/httpapi/planner_integration_test.go, apps/web/src/features/today/today.test.tsx."
    - "Do not modify Assessment, Planner production kernel, content/bootstrap fixtures, seed supply, Skill Leaf IDs, API/schema/generated bindings, DB schema/migrations/queries, auth, providers, workflows, validator/materializer code, CONSTITUTION.md, OBJECTIVE.md, docs/catalog/project.json, topology, main, tags, releases, or deployment."
    - "Do not add content supply or infer Band, mastery, readiness, learner weakness, score thresholds, calibration, recency, quantity, diversity, or confidence rules."
    - "Do not hand-edit generated semantic entries to force verification; use repository-native deterministic materialization only."
    - "Do not perform unrelated cleanup, formatting sweeps, framework expansion, or CI debugging loops."

structure_policy:
  expected_new_files:
    - .agent/tasks/TASK-0041/report.yaml
  allowed_existing_files:
    - .agent/tasks/TASK-0041/task.yaml
    - tools/canonical/generated/reading-training-registry.json
    - .github/verification-boundary/candidate
  unlisted_new_files:
    allowed: false
    max: 0
  allow_new_top_level_directories: false

invariants:
  - "Revision-1 semantic candidate bytes remain unchanged and continue to define TASK-0041 product semantics."
  - "Revision 2 is a deterministic generated-artifact correction, not a semantic redesign."
  - "Generated registry output must be reproducible from current canonical sources and must not contain manual semantic invention."
  - "The final verified candidate still contains the revision-1 Progression handoff semantics and no new product scope."
  - "No main mutation, promotion, release, branch creation, PR creation, or repository metadata mutation."

acceptance_criteria:
  - id: AC-1
    requirement: "Revision-1 semantic bytes are preserved exactly."
    evidence_required: "Diff from revision-2 handoff base to revision-2 candidate changes only tools/canonical/generated/reading-training-registry.json."
  - id: AC-2
    requirement: "Canonical generated artifact is deterministically rematerialized."
    evidence_required: "Repository-native materializer/check reports no generated artifact drift and registry provenance/current canonical-source revision match the current canonical source tree."
  - id: AC-3
    requirement: "Original TASK-0041 product semantics remain intact."
    evidence_required: "Exact candidate ancestry contains 84139d4433bf62635aa501c57c8bcb19dc8e0b3b unchanged; no seven semantic files are modified after that candidate."
  - id: AC-4
    requirement: "Authoritative verification passes on the new immutable candidate."
    evidence_required: "New sentinel-only boundary binds exact revision-2 candidate; detached checkout + exact HEAD assertion + ./verify all PASS."
  - id: AC-5
    requirement: "Terminal publication is report-only."
    evidence_required: "report.yaml is a direct child of the successful sentinel boundary and is its only changed file."

verification:
  executor_checks:
    - id: CHECK-1
      command_or_check: "Verify exact handoff base, topology {dev, main}, inherited revision-1 candidate/boundary/run identities, and current task revision 2."
      required: true
    - id: CHECK-2
      command_or_check: "Run repository-native canonical materialization/check locally on the execution surface if available; require the only tracked generated correction to be tools/canonical/generated/reading-training-registry.json."
      required: true
    - id: CHECK-3
      command_or_check: "Create one correction candidate; prove its diff from exact handoff base changes only the generated registry and preserves revision-1 semantic files byte-for-byte."
      required: true
    - id: CHECK-4
      command_or_check: "Create one sentinel-only verification child and require one new GitHub Actions verify run with exact binding, detached exact candidate HEAD, and ./verify PASS."
      required: true
    - id: CHECK-5
      command_or_check: "Publish report-only child and stop at NEEDS_REVIEW."
      required: true
  authoritative_verification:
    required: true
    mechanism: "repository-native GitHub Actions .github/workflows/verify.yml via sentinel boundary"
    expected_signal: "exact boundary->candidate binding PASS, detached exact candidate HEAD assertion PASS, canonical materialization PASS, and ./verify PASS"

capability_requirements:
  EXECUTION:
    - exact_remote_ref_read
    - repository_content_read
    - repository_git_object_write
    - non_force_ref_update
    - exact_diff_inspection
    - repository_native_deterministic_verification_execution
    - github_actions_run_read
  REVIEW:
    - exact_commit_report_resolution
    - repository_content_read
    - repository_content_write
    - github_actions_run_read

git_authority:
  create_branch: false
  commit: true
  push: true
  promote_to_main: false
  verification_boundary_child: true

release_authority:
  create_version_tag: false
  mutate_repository_metadata: false
  publish_release: false

blocking_decisions: []
execution_ready: true
