protocol_version: 3

task_id: TASK-0041
task_revision: 3
state: APPROVED

origin:
  type: correction
  task_id: TASK-0041
  gap_ids:
    - POST_SAMPLED_PROGRESSION_HANDOFF
    - GENERATED_CANONICAL_ARTIFACT_DRIFT
    - GENERATED_TRACE_ARTIFACT_DRIFT

architect_binding:
  target_repository: phatnguyen03022001/ilets

target:
  repository: phatnguyen03022001/ilets
  branch:
    name: dev
    role: primary-development

execution_base:
  mode: handoff_snapshot
  capture: after_final_planning_commit
  require_exact_match: true

skill_library:
  repository: phatnguyen03022001/agent-skills
  revision: "76f2d3877d1698f91eb5a6117e2301ed49480379"

execution_skills:
  required: [executor, simplicity, verification]
  recommended: []

inherited_evidence:
  semantic_candidate_rev1: "84139d4433bf62635aa501c57c8bcb19dc8e0b3b"
  failed_boundary_rev1: "b321b4aefab00a3b6247f16ed78bb7fbdb508eed"
  failed_actions_run_rev1: 33590220228
  rev1_failure: "canonical materialization failed: generated artifact drift: tools/canonical/generated/reading-training-registry.json"
  rev2_handoff_base: "6cd3d2c548329dc1a20df5c98ee691f0253f1cc2"
  rev2_result: AUTHORITY_REQUIRED
  rev2_additional_required_path: "tools/slice/generated/reading-training-trace.json"
  canonical_source_revision_before_materialization: "canonical-source-revision/v1:413fdd16ddb35e0861c8782bdef4999ee31cf746d9e210c721f33c9f82bd3bd3"
  canonical_source_revision_expected_from_current_owner_blobs: "canonical-source-revision/v1:0fc217f4357068c4e0c5014fc1730876b920d266da032ec882a23f476c9ba864"

objective: >-
  Complete TASK-0041 without changing the revision-1 product semantics by running the repository-native
  deterministic materializer and admitting exactly the two generated outputs that it is proven to render
  from one shared canonical_source_revision: the Reading training registry and the Reading training trace.
  Then verify the resulting exact immutable candidate through one new sentinel boundary and publish the
  canonical terminal report.

scope:
  required_changes:
    - "Refresh exact refs/topology/task and require dev equals the exact revision-3 handoff base with topology exactly {dev, main}."
    - "Preserve semantic candidate 84139d4433bf62635aa501c57c8bcb19dc8e0b3b exactly. Do not modify, amend, replay, reformat, reinterpret, or revert its seven semantic files."
    - "Run the repository-native deterministic canonical materializer against the current tree."
    - "Authorize exactly these generated tracked outputs from that materialization: tools/canonical/generated/reading-training-registry.json and tools/slice/generated/reading-training-trace.json."
    - "Require both outputs to share the same newly computed canonical_source_revision derived from the exact current canonical owner blobs."
    - "Require generated provenance to bind current canonical owners exactly; do not hand-edit generated semantic entries or revision fields."
    - "If materialization changes any tracked file beyond those exact two generated paths, stop AUTHORITY_REQUIRED and report the additional path set."
    - "Create exactly one revision-3 correction candidate from the exact handoff base. Its only non-task changes must be the two generated outputs above."
    - "Then create exactly one sentinel-only verification-boundary child changing .github/verification-boundary/candidate to the revision-3 correction candidate SHA."
    - "Require one new repository-native GitHub Actions verify run to bind that exact candidate, checkout detached, assert exact HEAD, and complete ./verify PASS."
    - "Do not rerun or mutate the failed revision-1 run; revision 3 uses one new exact verification boundary/run."
    - "Only after authoritative PASS publish exactly one report-only direct child at .agent/tasks/TASK-0041/report.yaml and stop at NEEDS_REVIEW."

  forbidden_changes:
    - "Do not modify docs/data/evidence-progression-content.md, docs/behavior/planner-and-lifecycle.md, services/core-api/internal/progression/sampled_reading.go, services/core-api/internal/progression/sampled_reading_test.go, services/core-api/internal/planner/kernel_test.go, services/core-api/internal/httpapi/planner_integration_test.go, or apps/web/src/features/today/today.test.tsx."
    - "Do not modify Assessment, Planner production kernel, content/bootstrap fixtures, seed supply, Skill Leaf IDs, public API/schema/generated API bindings, DB schema/migrations/queries, auth, providers, workflows, validator/materializer code, CONSTITUTION.md, OBJECTIVE.md, docs/catalog/project.json, topology, main, tags, releases, or deployment."
    - "Do not add content supply or infer Band, mastery, readiness, learner weakness, score thresholds, calibration, recency, quantity, diversity, or confidence rules."
    - "Do not hand-edit generated outputs to force verification; use repository-native deterministic materialization only."
    - "Do not perform unrelated cleanup, formatting sweeps, framework expansion, or iterative CI debugging loops."

structure_policy:
  expected_new_files:
    - .agent/tasks/TASK-0041/report.yaml
  allowed_existing_files:
    - .agent/tasks/TASK-0041/task.yaml
    - tools/canonical/generated/reading-training-registry.json
    - tools/slice/generated/reading-training-trace.json
    - .github/verification-boundary/candidate
  unlisted_new_files:
    allowed: false
    max: 0
  allow_new_top_level_directories: false

invariants:
  - "Revision-1 semantic bytes remain unchanged and continue to define TASK-0041 product semantics."
  - "Revision 3 is deterministic generated-artifact reconciliation only, not semantic redesign."
  - "Registry and trace are rendered together from one canonical_source_revision and must agree on that revision."
  - "No tracked path outside the exact generated pair may change in the correction candidate."
  - "No main mutation, promotion, release, branch creation, PR creation, or repository metadata mutation."

acceptance_criteria:
  - id: AC-1
    requirement: "Revision-1 semantic bytes remain exact."
    evidence_required: "No revision-1 semantic path changes after 84139d4433bf62635aa501c57c8bcb19dc8e0b3b."
  - id: AC-2
    requirement: "Repository-native materialization produces exactly the authorized generated pair."
    evidence_required: "Diff from revision-3 handoff base to correction candidate changes only tools/canonical/generated/reading-training-registry.json and tools/slice/generated/reading-training-trace.json."
  - id: AC-3
    requirement: "Generated provenance is coherent."
    evidence_required: "Registry and trace share canonical-source-revision/v1:0fc217f4357068c4e0c5014fc1730876b920d266da032ec882a23f476c9ba864 or the exact repository-native equivalent recomputed from unchanged current owner blobs, and provenance references current canonical source blobs."
  - id: AC-4
    requirement: "Original TASK-0041 product behavior remains intact."
    evidence_required: "Candidate ancestry preserves semantic candidate 84139d4433bf62635aa501c57c8bcb19dc8e0b3b and no later semantic mutation exists."
  - id: AC-5
    requirement: "Authoritative exact-candidate verification passes."
    evidence_required: "One new sentinel-only boundary binds the correction candidate; detached checkout, exact HEAD assertion, canonical materialization, and full ./verify all PASS."
  - id: AC-6
    requirement: "Terminal publication is report-only."
    evidence_required: "report.yaml is a direct child of the successful sentinel boundary and its only changed file."

verification:
  executor_checks:
    - id: CHECK-1
      command_or_check: "Verify exact revision-3 handoff base, topology {dev, main}, task revision 3, semantic candidate identity, failed rev1 evidence, and rev2 AUTHORITY_REQUIRED evidence."
      required: true
    - id: CHECK-2
      command_or_check: "Run repository-native materializer and require the tracked diff to contain exactly the registry and trace generated paths, with no third path."
      required: true
    - id: CHECK-3
      command_or_check: "Create one correction candidate and independently prove semantic paths unchanged and generated provenance/revision coherent."
      required: true
    - id: CHECK-4
      command_or_check: "Create one sentinel-only verification child and require one new GitHub Actions verify run with exact binding, detached exact candidate HEAD, and ./verify PASS."
      required: true
    - id: CHECK-5
      command_or_check: "Publish report-only child and stop at NEEDS_REVIEW."
      required: true
  authoritative_verification:
    required: true
    mechanism: "repository-native GitHub Actions .github/workflows/verify.yml via sentinel boundary"
    expected_signal: "exact boundary->candidate binding PASS, detached exact candidate HEAD assertion PASS, canonical materialization PASS, and ./verify PASS"

capability_requirements:
  EXECUTION:
    - exact_remote_ref_read
    - repository_content_read
    - repository_git_object_write
    - non_force_ref_update
    - exact_diff_inspection
    - repository_native_deterministic_verification_execution
    - github_actions_run_read
  REVIEW:
    - exact_commit_report_resolution
    - repository_content_read
    - repository_content_write
    - github_actions_run_read

git_authority:
  create_branch: false
  commit: true
  push: true
  promote_to_main: false
  verification_boundary_child: true

release_authority:
  create_version_tag: false
  mutate_repository_metadata: false
  publish_release: false

blocking_decisions: []
execution_ready: true
