protocol_version: 3

task_id: TASK-0041
task_revision: 5
state: APPROVED

origin:
  type: correction
  task_id: TASK-0041
  gap_ids:
    - POST_SAMPLED_PROGRESSION_HANDOFF
    - GENERATED_CANONICAL_ARTIFACT_DRIFT
    - GENERATED_TRACE_ARTIFACT_DRIFT
    - PRETTIER_FORMATTER_DRIFT
    - STALE_E2E_POST_SAMPLED_ORACLE

architect_binding:
  target_repository: phatnguyen03022001/ilets

target:
  repository: phatnguyen03022001/ilets
  branch:
    name: dev
    role: primary-development

execution_base:
  mode: handoff_snapshot
  capture: after_final_planning_commit
  require_exact_match: true

skill_library:
  repository: phatnguyen03022001/agent-skills
  revision: "76f2d3877d1698f91eb5a6117e2301ed49480379"

execution_skills:
  required: [executor, simplicity, verification]
  recommended: []

inherited_evidence:
  semantic_candidate_rev1: "84139d4433bf62635aa501c57c8bcb19dc8e0b3b"
  generated_candidate_rev3: "f92f51a35f45fc51737a5610f9670e65837ffaa0"
  formatter_candidate_rev4: "5fa2506f47b35edbf0a216e1e376d9cd4be93c05"
  failed_boundary_rev4: "b1f38e3a27c82c6f184415a612ba66e4b53b5541"
  failed_actions_run_rev4: 33599907707
  rev4_binding_result: "exact boundary binding PASS; detached exact candidate checkout PASS; exact HEAD assertion PASS"
  rev4_pre_e2e_result: >-
    Canonical/generated binding checks PASS, prettier --check . PASS, ESLint completed with warnings only,
    TypeScript PASS, Today Vitest 10/10 PASS, Next build PASS, Go unit/integration tests PASS, database
    migration/seed path PASS.
  rev4_failure: >-
    Playwright apps/web/e2e/reading-training.spec.ts timed out waiting for the obsolete post-submission
    TRANSITION/BLOCKED coverage gap at line 133.
  stale_oracle_diagnosis: >-
    Current Core behavior intentionally maps post-sampled admitted evidence plus prior sampled assignment
    to FreshSampleContentGap, materialized as CONTENT_OR_ASSET/content_assets/BLOCKED with zero plan items.
    The E2E still waits for the pre-TASK-0041 TRANSITION/progression_transition blocker, so the failing
    oracle is stale while focused runtime/unit/integration evidence is consistent with TASK-0041 semantics.

authority_sources:
  - source: "phatnguyen03022001/ilets@5fa2506f47b35edbf0a216e1e376d9cd4be93c05:services/core-api/internal/planner/kernel.go"
    role: "Current Planner decision authority: prior sampled assignment with authorized COLLECT_EVIDENCE routes to FreshSampleContentGap."
    precedence: 1
  - source: "phatnguyen03022001/ilets@5fa2506f47b35edbf0a216e1e376d9cd4be93c05:services/core-api/internal/httpapi/planner.go"
    role: "Current HTTP representation authority: FreshSampleContentGap is CONTENT_OR_ASSET/content_assets/BLOCKED."
    precedence: 2
  - source: "phatnguyen03022001/ilets@5fa2506f47b35edbf0a216e1e376d9cd4be93c05:services/core-api/internal/planner/kernel_test.go"
    role: "Focused test evidence that post-evidence prior assignment resolves to FreshSampleContentGap."
    precedence: 3
  - source: "GitHub Actions run 33599907707 attempt 1"
    role: "Authoritative exact-candidate proof that revision 4 reaches only the stale Playwright oracle after all preceding gates pass."
    precedence: 4

objective: >-
  Complete TASK-0041 by updating only the stale Playwright post-submission oracle to consume the already
  authoritative Core plan semantics established by the revision-1 product change: after sampled AT-02 evidence
  exists and the only bounded sample has already been assigned, the product exposes a fresh-content supply gap
  as CONTENT_OR_ASSET/content_assets/BLOCKED with no plan item. Do not alter product/runtime semantics.

scope:
  required_changes:
    - "Refresh exact refs/topology/task and require dev equals the exact revision-5 handoff base with topology exactly {dev, main}."
    - "Preserve product semantics from 84139d4433bf62635aa501c57c8bcb19dc8e0b3b, generated state from f92f51a35f45fc51737a5610f9670e65837ffaa0, and Today formatting state from 5fa2506f47b35edbf0a216e1e376d9cd4be93c05."
    - "Modify exactly apps/web/e2e/reading-training.spec.ts and only the stale post-submission oracle for the real Core plan."
    - "Replace the obsolete wait/assertion for gap_class TRANSITION / condition_id progression_transition with the current authoritative post-sampled supply-gap shape: gap_class CONTENT_OR_ASSET, condition_id content_assets, condition_status BLOCKED."
    - "Require the selected post-submission plan to have zero items and a non-empty blocking_consequence returned by Core."
    - "Require the E2E to verify the returned blocking_consequence is visible in Today and that the Recommended next heading is absent while the plan is blocked."
    - "Where asserted, bind stable current supply-gap metadata rather than long presentation copy: dependency fresh eligible sampled Reading assessment content and demand_class content/assets/supply route."
    - "Do not assert or infer Band, mastery, readiness, learner weakness, score threshold, evidence quantity, or any broader learner claim."
    - "Use repository-pinned Prettier on the changed E2E file after the oracle correction."
    - "Create exactly one revision-5 correction candidate from the exact handoff base. Its only non-task changed path must be apps/web/e2e/reading-training.spec.ts."
    - "Then create exactly one sentinel-only verification-boundary child changing .github/verification-boundary/candidate to the revision-5 candidate SHA."
    - "Require one new repository-native GitHub Actions verify run to bind the exact candidate, checkout detached, assert exact HEAD, and complete full ./verify PASS including the Playwright E2E."
    - "Do not rerun or mutate prior failed runs. Revision 5 gets one new exact verification boundary/run."
    - "Only after authoritative PASS publish exactly one report-only direct child at .agent/tasks/TASK-0041/report.yaml and stop at NEEDS_REVIEW."

  forbidden_changes:
    - "Do not modify production runtime, Planner, Progression, Assessment, Today implementation, Today unit tests, canonical docs, generated registry/trace, content/bootstrap fixtures, seed supply, Skill Leaf IDs, API/schema/generated bindings, DB schema/migrations/queries, auth, providers, workflows, validator/materializer code, formatter configuration, package/dependency versions, CONSTITUTION.md, OBJECTIVE.md, docs/catalog/project.json, topology, main, tags, releases, or deployment."
    - "Do not restore TRANSITION/progression_transition behavior merely to satisfy the stale E2E."
    - "Do not change unrelated E2E steps, answer submission, request capture, learner-safe assertions, test setup, or timing unless repository-pinned formatting requires byte layout changes only."
    - "Do not add fresh content supply in this task."
    - "Do not perform unrelated cleanup, formatting sweeps, framework expansion, or iterative CI debugging loops."

structure_policy:
  expected_new_files:
    - .agent/tasks/TASK-0041/report.yaml
  allowed_existing_files:
    - .agent/tasks/TASK-0041/task.yaml
    - apps/web/e2e/reading-training.spec.ts
    - .github/verification-boundary/candidate
  unlisted_new_files:
    allowed: false
    max: 0
  allow_new_top_level_directories: false

invariants:
  - "TASK-0041 product/runtime semantics remain unchanged by revision 5."
  - "The E2E oracle follows the current authoritative Core response instead of forcing the superseded transition blocker."
  - "Post-sampled no-fresh-supply state is represented as CONTENT_OR_ASSET/content_assets/BLOCKED with zero items."
  - "Revision 5 does not manufacture broader learner conclusions or content supply."
  - "No main mutation, promotion, release, branch creation, PR creation, or repository metadata mutation."

acceptance_criteria:
  - id: AC-1
    requirement: "Correction is test-oracle-only."
    evidence_required: "Diff from revision-5 handoff base to candidate changes only apps/web/e2e/reading-training.spec.ts; no production/runtime/docs/generated path changes."
  - id: AC-2
    requirement: "E2E consumes the authoritative post-sampled Core blocker."
    evidence_required: "After real AT-02 submission the E2E observes CONTENT_OR_ASSET/content_assets/BLOCKED, zero plan items, a non-empty Core blocking_consequence rendered by Today, and no Recommended next heading."
  - id: AC-3
    requirement: "No unsupported learner inference is introduced."
    evidence_required: "E2E and unchanged runtime make no Band, mastery, readiness, weakness, threshold, or broader evidence-sufficiency claim beyond the scoped supply blocker."
  - id: AC-4
    requirement: "Authoritative exact-candidate verification passes."
    evidence_required: "One new sentinel-only boundary binds the revision-5 candidate; detached checkout, exact HEAD assertion, canonical materialization, formatting checks, focused tests, Playwright E2E, and full ./verify all PASS."
  - id: AC-5
    requirement: "Terminal publication is report-only."
    evidence_required: "report.yaml is a direct child of the successful sentinel boundary and its only changed file."

verification:
  executor_checks:
    - id: CHECK-1
      command_or_check: "Verify exact revision-5 handoff base, topology {dev, main}, task revision 5, inherited candidate identities, and rev4 exact failure evidence."
      required: true
    - id: CHECK-2
      command_or_check: "Update only the stale post-submission Playwright oracle; verify it targets CONTENT_OR_ASSET/content_assets/BLOCKED with zero items and renders Core blocking_consequence; format only that file with repository-pinned Prettier."
      required: true
    - id: CHECK-3
      command_or_check: "Create one correction candidate and prove its only non-task changed path is apps/web/e2e/reading-training.spec.ts."
      required: true
    - id: CHECK-4
      command_or_check: "Create one sentinel-only verification child and require one new GitHub Actions verify run with exact binding, detached exact candidate HEAD, and full ./verify PASS including Playwright."
      required: true
    - id: CHECK-5
      command_or_check: "Publish report-only child and stop at NEEDS_REVIEW."
      required: true
  authoritative_verification:
    required: true
    mechanism: "repository-native GitHub Actions .github/workflows/verify.yml via sentinel boundary"
    expected_signal: "exact boundary->candidate binding PASS, detached exact candidate HEAD assertion PASS, canonical materialization PASS, prettier PASS, Playwright E2E PASS, and full ./verify PASS"

capability_requirements:
  EXECUTION:
    - exact_remote_ref_read
    - repository_content_read
    - repository_git_object_write
    - non_force_ref_update
    - exact_diff_inspection
    - repository_pinned_formatter_execution
    - repository_native_deterministic_verification_execution
    - github_actions_run_read
  REVIEW:
    - exact_commit_report_resolution
    - repository_content_read
    - repository_content_write
    - github_actions_run_read

git_authority:
  create_branch: false
  commit: true
  push: true
  promote_to_main: false
  verification_boundary_child: true

release_authority:
  create_version_tag: false
  mutate_repository_metadata: false
  publish_release: false

blocking_decisions: []
execution_ready: true
